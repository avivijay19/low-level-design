constructors with too many parameters

When a class has a large number of parameters, especially if many of them are optional, it can lead to constructors that are difficult to read and use. This is often referred to as the "telescoping constructor" anti-pattern.